import { Canvas, Meta, ArgTypes } from '@storybook/addon-docs/blocks';
import * as TextareaStories from '../Textarea.stories';

<Meta of={TextareaStories} />

# Textarea
Simple multi-line text input.
<Canvas of={TextareaStories._DocsIntro} sourceState={'shown'} />

## Disabled
Prevent users from interacting with the textarea by disabling it.
<Canvas of={TextareaStories._Disabled} sourceState={'shown'} />

## Elastic
Allow the textarea to adjust its size based on the content.
<Canvas of={TextareaStories._Elastic} sourceState={'shown'} />

## Fixed Height
Set the height of the textarea by passing the `rows` prop.
<Canvas of={TextareaStories._FixedHeight} sourceState={'shown'} />

## Placeholder
Add a placeholder to the textarea.
<Canvas of={TextareaStories._Placeholder} sourceState={'shown'} />

## Resize Controls
If you want to restrict the user from resizing the textarea, you can use the `resize` prop.
<Canvas of={TextareaStories._ResizeControls} sourceState={'shown'} />

## Copyable
Add a copy button to your textarea by passing the `copyable` prop.
<Canvas of={TextareaStories._Copyable} sourceState={'shown'} />

## Arguments
<ArgTypes of={TextareaStories} />
