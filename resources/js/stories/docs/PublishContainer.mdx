import { Canvas, Meta, ArgTypes } from '@storybook/addon-docs/blocks';
import * as PublishContainerStories from '../PublishContainer.stories';

<Meta of={PublishContainerStories} />

# Publish Container
The `PublishContainer` component is used to create Publish Forms in the Control Panel. It handles field rendering, validation, localization and state management.

<figure>
    <picture>
        <source srcset="/img/publish-form-dark.webp" media="(prefers-color-scheme: dark)" />
        <img src="/img/publish-form.webp" alt="Screenshot of a custom Publish Form" />
    </picture>
</figure>

To learn more about the `PublishContainer` component (and publish forms in general), visit the ["Publish Forms"](https://statamic.dev/control-panel/publish-forms) page on our main documentation site.