// Autocompletable categories supported by JS command palette actions.
// This intentionally doesn't reflect the full list of categories
// provided by the server, because things like navigation and
// content search are only ever to be provided by server.
export const CATEGORY = {
    Actions: 'Actions',
    Miscellaneous: 'Miscellaneous',
};
